# Database
database:
    username: root
    password: Di19Mp78!
    name: framework
    default_object_namespace: \Application\Domain\Entity

# Logging
log:
    path: application/log

# Templating
templating:
    default: page.php